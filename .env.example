# Database (dùng khi chạy app trên host, kết nối tới Docker)
MYSQL_DSN=root:crawl_secret@tcp(localhost:3306)/story?parseTime=true
MYSQL_ROOT_PASSWORD=crawl_secret
MYSQL_DATABASE=story

# Redis
REDIS_ADDR=localhost:6379

# Server port (server-go)
PORT=8081
